export interface QueryType {
    sortType: string
    priceValue: number
    tags: string[]
    weightQuery: string[]
    brandsQuery: string[]
}
export interface filters {
    properties: string[]
    brands: string[]
}